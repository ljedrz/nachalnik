Performance and code-quality review of kamchatka/src/app/ (all files). Focus: work done per kernel event and per streamed chunk (rebuilding the transcript, views or Going from scratch, re-reading the whole context from the kernel, cloning items), per keystroke work in the input handling, and anything O(n^2) in session length. Quality: functions doing several unrelated things, duplicated logic between app files, state that can disagree with the kernel, dead code.
