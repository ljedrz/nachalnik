Cross-cutting code-quality review of the whole workspace. Focus: helpers duplicated across crates or modules (text truncation, byte/char/width helpers, path handling, JSON helpers, time formatting) that have drifted apart; public items that nothing uses (grep for each pub fn / pub struct in kamchatka and the eval crate); the AGENTS.md rule "one word per mechanism" broken in enums, help text or messages (truncate/elide/exclude/supersede used interchangeably); comments that describe mechanics instead of decisions or contradict the code; tests helpers duplicated between test files instead of shared.
