Code-quality review of the examples across the workspace: nachalnik/examples, kamchatka/examples, nachalnik-mcp/examples (if any) and nachalnik-providers/examples (if any). Focus: examples that no longer demonstrate what their doc comment says, helpers duplicated between examples instead of living in the shared example module, examples that swallow errors or unwrap where the point of the example is showing how to handle the failure, examples that use an API in a way the crate docs say not to, and dead code. Check which examples CI runs (.github/workflows/ci.yml) and whether their claimed behaviour matches.
