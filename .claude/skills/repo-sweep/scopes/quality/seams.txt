Performance and code-quality review of nachalnik/src/selectors.rs, nachalnik/src/permissions.rs, nachalnik/src/tool.rs, nachalnik/src/config.rs and nachalnik/src/test.rs. Focus: selector parsing and matching cost over large contexts (re-parsing per item, O(n^2) matching for first/latest), permission table lookups done linearly per call, clones of ToolSpec or schemas per call, test helpers in test.rs that behave differently from the real seams they stand in for, dead code, docs that disagree with the code. Remember the core crate's rule: nothing that can live outside it goes in, so a finding that adds a feature to the core is not a finding.
