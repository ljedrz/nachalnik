Performance and code-quality review of nachalnik-mcp/src (all files). Focus: per call work (schema conversion or tool list rebuilt per call, JSON cloning of large results, stderr capture buffers), locks held across awaits, polling loops. Quality: duplicated conversion logic, dead code, error paths that lose the server's message, names that say something other than what the code does.
