Performance and code-quality review of nachalnik/src/context.rs, projection.rs, compaction.rs, tokens.rs, event.rs, session.rs, selectors (if present) and model.rs. Focus: linear scans by id where a lookup is done repeatedly (O(n^2) over items in a long session), checkpoints and undo copying the whole context, token counting redone for unchanged items, event payloads cloning content, session save/load serialisation cost. Quality: duplicated logic, dead code, docs that contradict the code.
