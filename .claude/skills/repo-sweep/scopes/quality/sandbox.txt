Performance and code-quality review of kamchatka/src/sandbox.rs and the spawn path in kamchatka/src/tools/shell.rs (how a command is re-executed confined, how the confinement travels on the command line, process groups, the scratch directory). Focus: work repeated per shell call that could be done once per session (path canonicalisation, reading git config, building rulesets), blocking filesystem calls inside async code on the call path, duplicated path logic between sandbox.rs and the Reach checks, error paths that lose the OS error, dead code, docs that disagree with the code.
