Performance and code-quality review of nachalnik/src/kernel/ (mod.rs, request.rs, calls.rs). Focus: the work done on every Kernel::step and every public operation - how often the context is projected, counted and cloned per step, whether preview_request and the actual request duplicate work, lock scope (work done under the kernel lock, events emitted), Vec/HashMap rebuilt per call, clones of Content or ContextItem where Arc sharing is the rule (INVARIANTS.md: Content is shared, not copied). Quality: functions doing several things, duplicated code between the halves, error paths that lose information.
