Performance and code-quality review of kamchatka/src/tools/ and kamchatka/src/introspect/ (all files). Focus: fs grep and glob on large trees (reading whole files into memory, walking ignored directories, regex compiled per file, no early stop at the result limit), read of large files, edit's matching cost, shell output handling, and the introspection tools projecting or serialising the whole context per call. Quality: duplicated path handling, error messages to the model that lose the cause, dead code.
