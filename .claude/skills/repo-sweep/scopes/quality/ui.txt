Performance and code-quality review of kamchatka/src/ui/ (all files). Focus: the work done on every frame draw and every keystroke - is the whole transcript re-wrapped, re-parsed as markdown, re-measured or re-highlighted each frame even when nothing changed; allocations proportional to the whole session per frame; width computations repeated per line. Estimate the cost for a session of a few thousand items and long tool outputs. Quality: drawing code that makes decisions (AGENTS.md: ui draws and decides nothing), duplicated layout helpers, dead code.
