Performance and code-quality review of nachalnik-providers/src (all files). Focus: per streamed chunk work in the SSE reader and the two dialects' stream decoders (allocations per line, re-scanning buffers, string concatenation that is quadratic in response length, JSON re-parsing), request building (copying every message's content or base64-encoding blobs again on every request and retry), retry and backoff paths. Quality: logic duplicated between the OpenAI and Gemini dialects that has drifted, error messages that lose the cause, dead code.
