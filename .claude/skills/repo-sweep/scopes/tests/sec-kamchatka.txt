Test-coverage review of kamchatka's security claims, reading tests only as evidence: SECURITY.md (read all of it first) states what is enforced. For each enforced claim in it - the Landlock grants, the TCP rule, the unix-socket rule, the fs reach and `openat2` beneath, the leading `~` refusal, links past a path rule, the strictest-rule-wins property, exact path-rule names, keys stripped from the shell's environment, the gate's held and refused calls, a post-call network attempt refused, the permissions tab saying when confinement is partial or absent, `--serve` refusing non-loopback, the socket's 0600, the record directory's 0700 check, the relays' host and origin checks, the output ceiling - find the test that would fail if the claim stopped being true, in kamchatka/tests/ (sandbox.rs, boundary.rs, policy.rs, files.rs, search.rs, ceiling.rs, headless.rs, remote/, config.rs) or in a `#[cfg(test)]` module under kamchatka/src/, and quote its assertion. Report each claim as PINNED (test name and the assertion) or UNPINNED (what a test would need to do), and each test that claims to pin something but would still pass if the claim broke. Do not report production-code defects; this scope is the tests.
