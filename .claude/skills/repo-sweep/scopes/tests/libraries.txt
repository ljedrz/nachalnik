Code-quality review of TEST CODE ONLY. Findings about production code are out of scope and must not be reported; a production issue you happen to notice goes in one line at the very end, marked OUT OF SCOPE. Focus, and look at every test in the scope for each of these: (1) assertions that cannot fail - asserting on a value the test itself just set, a loop over a collection that may be empty so the assert never runs, a match or `is_ok() || is_err()` that accepts any outcome, an `assert!(x.contains(""))`; (2) tests whose name or doc comment claims something the body does not check; (3) assertion messages that contradict the condition; (4) helpers duplicated between test files that have drifted apart; (5) tests that would still pass if the behaviour they are named after were removed (say which line of production code you would delete and why the test still passes). Fixed sleeps have already been reviewed; skip them. Report each with file:line and the exact reason. Scope: nachalnik/tests, nachalnik-providers/tests, nachalnik-mcp/tests and nachalnik-eval/tests.
