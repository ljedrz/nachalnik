Security audit of how kamchatka/src/tools/shell.rs runs a command, with kamchatka/src/endpoint.rs (`KEYS`) and the spawn path in kamchatka/src/sandbox.rs, against SECURITY.md lines 77-88, 100-124 and 203-205 (read those first). Check, quoting the deciding lines: that every variable in `endpoint::KEYS` is removed from the command's environment on every path, confined or under `--no-sandbox`, and whether any other secret-bearing variable this program reads is left in; that the command is the process a stop kills (the process group, what happens to children that leave it or outlive the call, and whether a stopped call's output keeps arriving); the output ceiling `tools::KEPT` on stdout and stderr together and on a stream with no newlines; `Sandbox::note_for` and `Reach::range` - whether a refusal is only annotated where the confinement caused it, names every path the session reaches, and names no path but the one refused; and whether the working directory or TMPDIR a command is given can be pointed outside the reach by anything a model controls. Report verified defects only.
