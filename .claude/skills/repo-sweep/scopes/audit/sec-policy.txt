Security audit of the permission policy, `Careful` in kamchatka/src/tools/policy.rs, and how its decisions are applied in kamchatka/src/app/ (`App::decide`, `App::decide_reach`) and kamchatka/src/wiring.rs, against SECURITY.md lines 14-33 and 149-156 (read those first). Check, quoting the deciding lines: that the strictest of everything consulted wins in every combination of capability, path and operation rules, so no rule can reopen what another refused; that path rules compare names exactly and are matched against the path as the tool will resolve it (a `./`, a `..`, a trailing slash, a doubled slash, an absolute spelling of a relative rule); that a link past a path rule is refused by `fs`; the credential-shaped default rules and whether every `fs` operation and `shell` is judged by them; a call whose operation cannot be placed being widened to every capability the tool has; a rule naming a domain or operation nothing is judged under being refused where it is given; MCP tools judged under their server's name; what `always` records and whether it can grant more than was consulted; and that all three loops (screen, headless, served) answer through the same function. Report verified defects only: a call the documented rules refuse that the code lets run, or a documented claim the code does not keep.
