Security audit of how a session is served and recorded: kamchatka/src/remote/server.rs (the bind), the record directory in kamchatka/src/wiring.rs, and the relays in kamchatka/examples/ (gateway and phone), against SECURITY.md lines 140-148, 181-188 and 197-202 (read those first). Check, quoting the deciding lines: that `--serve` refuses every non-loopback address, including IPv6 forms, IPv4-mapped IPv6, `0.0.0.0`, a hostname that resolves to something else, and a unix path; when and how the socket file is made 0600 and what a pre-existing file or link at that path does; the record directory's checks (0700, owner, what happens if the name is a link, a file, or a directory another user owns) and whether a failed check writes nothing; the relays' checks on host, origin and content type, and whether a request from a web page in a browser on the same machine can pass them (a missing Origin, `null`, a DNS name that resolves to 127.0.0.1, a simple request that needs no preflight). Report verified defects only.
