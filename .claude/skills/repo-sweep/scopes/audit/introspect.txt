Correctness audit of kamchatka/src/introspect/ (context/, log.rs, fork, setup): every action's effect on the kernel, the model's own undo journal versus the person's undo stack, pins the model made versus pins a person made, what is refused and whether the refusal holds, the figures reported against what the kernel says, and the fork's copy of the session. Report verified defects only.
