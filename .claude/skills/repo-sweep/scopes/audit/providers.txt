Correctness audit of nachalnik-providers/src (openai/, gemini.rs, reading.rs, waiting.rs): assembling streamed tool calls (indices, ids arriving late, arguments split across chunks, several calls interleaved), reasoning and content handling, usage accounting, stop reasons, retries that can resend a request the server already acted on, interruption mid-stream, and what each dialect sends for every Message/Content/Block shape the kernel can produce. Report verified defects only.
