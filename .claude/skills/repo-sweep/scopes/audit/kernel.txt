Correctness audit of nachalnik/src/kernel/ (mod.rs, request.rs, calls.rs): the state machine and its transitions (a second concurrent step, a dropped step future, Restore guards), deciding and cancelling pending calls, running calls one at a time and in parallel, interrupts, undo/redo checkpoints and batches, events emitted under the lock that made the change, and every public operation's error paths. Check each against INVARIANTS.md. Report verified defects only.
