Security and correctness audit of kamchatka/src/tools/ (fs.rs, search.rs, shell.rs, ops.rs, trim.rs and the Careful policy): path checks and symlinks, what the policy lets through for each capability, argument validation, output limits and truncation, the compactor's plan, and anything a model's tool call could do that the documented permissions say it cannot. Report verified defects only.
