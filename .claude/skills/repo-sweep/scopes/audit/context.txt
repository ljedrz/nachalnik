Correctness audit of nachalnik/src/context.rs, projection.rs, compaction.rs, session.rs and event.rs against INVARIANTS.md: nothing destroyed, identifiers never reused (including across a resumed snapshot), a pin is a promise, one operation one undo, the previewed request is the request, call/result pairing repairs, snapshot problems() validation, and serde compatibility of what is written to disk. Report verified defects only.
