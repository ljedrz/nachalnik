Security audit of how the `fs` tool is held to the session's reach: `Reach` in kamchatka/src/sandbox.rs (`allows`, `range`, `open`, `replace`, `beneath`, `overwrite`) and its callers in kamchatka/src/tools/files.rs and kamchatka/src/tools/search.rs, against SECURITY.md lines 46-54 and 125-133 (read those first). Check, quoting the deciding lines: a path with `..`, an absolute path, a path through a symlink or to one, a symlink whose target is outside the reach, a hard link, a path naming a directory or a device or a fifo, a component swapped between the check and the open (is every open that follows a check done beneath the allowed directory, and what happens on a kernel without `openat2`), what `replace` does with the temporary file and the rename and the fallbacks it lists, the leading-`~` refusal and whether it comes before every early return under `--no-sandbox`, and whether `grep` and `glob` walks can descend through a link to a directory outside the reach or report content from outside it. Also: do `read`, `edit` and the walks all go through the same check, or does any path reach the filesystem another way. Report verified defects only: a way for an `fs` call to read, write or list something outside the reach it reports, or a documented claim the code does not keep.
