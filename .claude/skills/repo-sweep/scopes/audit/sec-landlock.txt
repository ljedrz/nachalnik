Security audit of the shell's Landlock confinement in kamchatka/src/sandbox.rs, against SECURITY.md lines 34-42 and 89-99 (read those first; they are the claims under audit). Check, quoting the lines that decide each: exactly which paths the ruleset grants read-write and read-only (the working directory, the system directories, `--sandbox-allow`, `--sandbox-read`, the scratch directory handed over as TMPDIR, a repository's `.git`) and whether any grant is wider than the prose says - a parent directory, a path that resolves through a link to somewhere else, a path given relative, an empty or `/` entry, a `~` in a settings file; which ABI each right needs and what the code does when the kernel lacks it (the `Partial` and not-enforced outcomes, whether every one of them reaches a `Confinement` variant the permissions tab draws, and whether any path degrades to unconfined without saying so); the TCP bind and connect rules; `confines_unix_sockets` and whether handling the unix-socket right is decided by asking the kernel rather than assuming; the re-exec that restricts itself and then `exec`s the command, and anything done between the restriction and the `exec` that the confined command could influence. Report verified defects only: a claim in SECURITY.md the code does not keep, or a grant wider than documented.
