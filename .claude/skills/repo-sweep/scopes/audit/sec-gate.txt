Security audit of the network gate: kamchatka/src/gate.rs, kamchatka/src/tools/reaching.rs and where kamchatka/src/tools/shell.rs and kamchatka/src/sandbox.rs install and serve it, against SECURITY.md lines 55-76 (read those first). Check, quoting the deciding lines: the seccomp filter program - the architecture check, the native, 32-bit and x32 syscall tables, which calls are held (`socket` for AF_INET and AF_INET6, reading only the low 32 bits of the family) and which are refused outright (`io_uring_setup`), and whether any other call a confined process could use to get an internet socket is left open (for example `socketpair`, `socketcall` on 32-bit, `io_uring` through another route) - and the default action for everything else; how the listener is handed to the parent and what happens if that fails; that each answer is per command and what happens to an attempt made after the call ended; that `deny` is `EACCES`; what happens when the filter cannot be installed and whether that is reported as `network not gated` rather than silently; the `unsafe` blocks and whether each one's stated reason holds. Report verified defects only: a way for a confined command to get an internet socket the gate did not answer for, or a documented claim the code does not keep.
