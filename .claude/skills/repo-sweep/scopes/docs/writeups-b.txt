Documentation check of the write-ups docs/retracting-a-hallucination/index.html and docs/taking-away-the-receipt/index.html. They are accounts of past sessions, so a past event is not a defect; a defect is a present-tense claim about how kamchatka or the runtime works (a tool, an action, a flag, a key, a file, a mechanism) that the code at HEAD contradicts, or a link to a page or repository file that does not exist. For each claim, open the code that decides it before judging. Report verified defects only.
