Documentation check of nachalnik/README.md and nachalnik/CONCEPTS.md against nachalnik/src. Every code example must compile against the current API: check each type, method, argument order, return type and feature it uses (nachalnik/src/lib.rs re-exports, kernel/mod.rs, context.rs, model.rs, tool.rs, permissions.rs, tokens.rs, compaction.rs, projection.rs, event.rs, session.rs, selectors.rs, and the features in nachalnik/Cargo.toml). Every concept's description must match the mechanism: the states and transitions, what undo takes, what an event carries, what a compactor may do, what the counter reports. The two documents must not contradict each other or INVARIANTS.md. For each claim, open the code that decides it before judging. Report verified defects only.
