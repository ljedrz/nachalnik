Documentation check of CONTRIBUTING.md lines 1 to 267 (the commands section) command by command: every script, flag, feature, example, test file, environment variable, endpoint and model named must exist and do what is said - check scripts/*.sh (read each script), .github/workflows/ci.yml, every Cargo.toml's [features] and [[example]]/[[test]] entries, nachalnik-utils/src/lib.rs (every variable and default it reads), and each live suite (nachalnik/tests/live.rs, nachalnik-providers/tests, nachalnik-eval/tests/live.rs, kamchatka/tests/live.rs: what makes it skip, which variables it reads). For each claim, open the code that decides it before judging. Report verified defects only.
