Documentation check of the doc comments, note: paragraphs and in-body comments in nachalnik-providers/src/openai/ (every file) and src/reading.rs, src/waiting.rs. For each: does it say what the code does - what is sent for each Content kind, how a stream is read and what ends it, retry and backoff and their constants, what an interrupt does, which fields are copied from params; do intra-doc links resolve; do comments contradict the code next to them. For each claim, open the code that decides it before judging. Report verified defects only.
