Documentation check of nachalnik-eval/RUNNING.md alone, line by line, against nachalnik-eval/examples (bench.rs, compare.rs, pool.rs: every flag, environment variable, default, output file and printed field), nachalnik-utils/src/lib.rs, nachalnik-eval/Cargo.toml and src/experiment.rs (report shape). For each claim, open the code that decides it before judging. Report verified defects only.
