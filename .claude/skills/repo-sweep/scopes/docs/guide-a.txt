Documentation check of kamchatka/GUIDE.md from its start up to the heading 'the permissions tab' (lines 1 to about 377): the four tabs, what the model writes, the rest of the keys. Check every key binding against kamchatka/src/app/keys.rs and app/mod.rs, every slash command against app/command.rs and help.rs, what each view shows against ui/ (mod.rs, tabs.rs, overlay.rs, markdown.rs, table.rs), and search against app/search.rs. Failure classes: a key that does something else or nothing, a command with the wrong name or arguments, a description of a view that no longer matches what is drawn. For each claim, open the code that decides it before judging. Report verified defects only.
