Documentation check of the latest released section of each changelog: kamchatka/CHANGELOG.md [0.15.1], nachalnik/CHANGELOG.md [0.7.1], nachalnik-providers/CHANGELOG.md [0.6.1], nachalnik-eval/CHANGELOG.md [0.5.1] and nachalnik-mcp/CHANGELOG.md [0.7.1]. A released entry is history, so it is a defect only if it names something wrongly for the code of that release; the useful check is the other way round: for each released bullet whose behaviour the code at HEAD no longer has, the crate's [unreleased] section must say so (nachalnik-mcp has no unreleased section, so any change there since is undocumented). Also check the link references at the bottom of each file and the Keep a Changelog headings. For each claim, open the code that decides it before judging. Report verified defects only.
