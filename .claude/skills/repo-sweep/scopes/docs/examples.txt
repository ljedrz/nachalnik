Documentation check of the examples and their prose: nachalnik/examples/*.rs and nachalnik/examples/common, and every place a README, CONTRIBUTING.md or CI names an example. Check: each example's module doc says what it does and how to run it (features, environment variables from nachalnik-utils/src/lib.rs, whether it needs a key), the command lines elsewhere use the right example names and features (nachalnik/Cargo.toml [[example]] entries), and the comments inside the examples match the code next to them. For each claim, open the code that decides it before judging. Report verified defects only.
