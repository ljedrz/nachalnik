Documentation check of nachalnik-providers: README.md, the crate docs in src/lib.rs, and the doc comments and note: paragraphs in src/openai/, src/gemini.rs, src/endpoint.rs, src/reading.rs, src/waiting.rs, src/markup.rs, src/system1.rs, src/conformance.rs. Check: features named and what each gates (Cargo.toml), retry and backoff defaults and limits described against the constants, what is sent for each Content kind in each dialect, how interruption works, environment variables, code examples against the actual API, and CHANGELOG.md's unreleased section against the code. For each claim, open the code that decides it before judging. Report verified defects only.
