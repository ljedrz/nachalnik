Documentation check of the write-ups under docs/: docs/index.html, docs/a-lie-in-its-own-notes/index.html, docs/an-experiment-on-itself/index.html and docs/putting-words-in-its-mouth/index.html. They are accounts of past sessions, so a past event is not a defect; a defect is a present-tense claim about how kamchatka or the runtime works (a tool, an action, a flag, a key, a file, a mechanism) that the code at HEAD contradicts, a link to a page or repository file that does not exist, or index.html listing a page that is not there or missing one that is. For each claim, open the code that decides it before judging. Report verified defects only.
