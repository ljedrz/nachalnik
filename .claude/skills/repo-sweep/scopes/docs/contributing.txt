Documentation check of CONTRIBUTING.md against the workspace. Check: every command and its flags (does the script or feature exist: scripts/*.sh, the features in each Cargo.toml), what it says CI does (.github/workflows/*.yml), the environment variables each live suite and example reads (nachalnik-utils/src/lib.rs, the tests under */tests that skip without a key), the endpoints and models it names, the conventions against what AGENTS.md says (the two must not contradict each other), and every gotcha's description of a mechanism (for example emitting an event under the lock: check nachalnik/src/kernel/mod.rs does it the way described). For each claim, open the code that decides it before judging. Report verified defects only.
