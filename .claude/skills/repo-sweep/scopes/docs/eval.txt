Documentation check of nachalnik-eval: README.md, RUNNING.md, src/lib.rs crate docs, and the doc comments and note: paragraphs in src/ (experiment.rs, score.rs, probe.rs, trial.rs, subject.rs, intervene.rs, fork.rs, abreast.rs, suite/) and examples/ (bench.rs, compare.rs, pool.rs: their flags, environment variables and output files). Check: the experiments the docs list against suite/mod.rs, how each is scored against score.rs, the command lines in RUNNING.md against the examples' argument parsing, report field names, and the unreleased CHANGELOG.md section against the code. For each claim, open the code that decides it before judging. Report verified defects only.
