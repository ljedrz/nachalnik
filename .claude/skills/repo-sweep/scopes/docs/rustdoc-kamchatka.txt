Documentation check of the doc comments and note: paragraphs in kamchatka/src: app/ (mod.rs, command.rs, going.rs, keys.rs, search.rs, text.rs, transcript.rs, views.rs, when.rs), ui/, wiring.rs, main.rs, lib.rs, headless.rs, stopping.rs, endpoint.rs, mcp.rs, sandbox.rs, gate.rs. For each: does it say what the code does now (return values on each branch, what is refused, defaults), do intra-doc links resolve, do module docs describe the module's current contents, and do comments inside bodies contradict the code next to them. For each claim, open the code that decides it before judging. Report verified defects only.
