Documentation check of POSTPONED.md from line 235 (the entry 'A projection larger than protocol::MAX_LINE') to its end. For each entry: is what it says the code does still what the code does - open the files, types and functions it names (grep for each) and quote the lines; is the thing it says is not built still not built (if it has been built, the entry is stale); do the names it uses (types, functions, flags, files, tests) exist under those names; does the 'what would unblock it' still describe a real gap. For each claim, open the code that decides it before judging. Report verified defects only.
