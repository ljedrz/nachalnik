Documentation check of kamchatka/GUIDE.md lines 469 to 700 only (the question itself, a command that reaches for the network, finding things without a shell, putting something in). Check each answer to a question and what it does against kamchatka/src/app/keys.rs, app/mod.rs, tools/policy.rs and gate.rs; the network gate against gate.rs and sandbox.rs; glob, grep and read against tools/search.rs, tools/fs.rs and tools/files.rs (limits, caps, defaults, output shape); attachments against attach.rs, clipboard.rs and app/command.rs. For each claim, open the code that decides it before judging. Report verified defects only.
