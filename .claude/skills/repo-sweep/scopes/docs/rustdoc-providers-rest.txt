Documentation check of the doc comments, note: paragraphs and in-body comments in nachalnik-providers/src/gemini.rs, endpoint.rs, markup.rs, system1.rs, conformance.rs and lib.rs. For each: does it say what the code does - what is sent for each Content kind, thinking and its signatures, how the endpoint is chosen, what conformance checks; do intra-doc links resolve; do comments contradict the code next to them. For each claim, open the code that decides it before judging. Report verified defects only.
