Documentation check of CONTRIBUTING.md from line 268 to its end (conventions and gotchas). For each convention, check it against AGENTS.md's short form (the two must agree) and check every example of the convention it quotes from the code exists as quoted (grep for it). For each gotcha, check the mechanism it describes is still how the code works (open the file it names) and that commands or scripts it names exist with those flags. For each claim, open the code that decides it before judging. Report verified defects only.
