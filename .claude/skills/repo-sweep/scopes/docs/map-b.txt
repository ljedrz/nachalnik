Documentation check of MAP.md from line 111 to its end, sentence by sentence: every file, module, type, function, feature and test named must exist under that name where it says (grep and glob for each), and every description of what a file does or which file owns a responsibility must match the file - read the module doc and the items it names. For each claim, open the code that decides it before judging. Report verified defects only.
