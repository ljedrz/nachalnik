Documentation check of INVARIANTS.md against nachalnik/src and nachalnik/tests. For each invariant: does the code enforce what it says (quote the enforcing line), does every type, function, event name (for example context.replaced), error variant and test file it names exist under that name, and does its reasoning describe the current mechanism rather than an older one. Also check that AGENTS.md's short list and INVARIANTS.md say the same things. For each claim, open the code that decides it before judging. Report verified defects only.
