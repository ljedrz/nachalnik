Documentation check of nachalnik/CONCEPTS.md alone, section by section: context as a data structure, a turn keeps its order, the budget, stopping, events, sessions. For every mechanism described, open the code in nachalnik/src (context.rs, model.rs, tokens.rs, kernel/, event.rs, session.rs, compaction.rs, projection.rs) and check it: names of types, methods, variants and events, what is kept and what is dropped, what is estimated and how, what a resumed session restores. Every code snippet must match the current API. For each claim, open the code that decides it before judging. Report verified defects only.
