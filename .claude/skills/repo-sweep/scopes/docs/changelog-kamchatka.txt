Documentation check of the [unreleased] section of kamchatka/CHANGELOG.md alone (lines 8 to 171), bullet by bullet: open the item each bullet names and check its name, its behaviour and any default or limit it states; flag bullets that name a type, function, flag, key, tool action or file that does not exist under that name, and bullets that contradict each other or a later commit (git history is not available to you; judge against the code). For each claim, open the code that decides it before judging. Report verified defects only.
