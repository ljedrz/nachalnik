Documentation check of the doc comments, note: paragraphs and in-body comments in kamchatka/src/introspect/ (mod.rs, context/mod.rs, context/changes.rs, fork.rs, log.rs, setup.rs). For each: does it say what the code does now - actions, arguments, what is refused and why, what a fork sees, what undo leaves; do intra-doc links resolve; do comments in bodies contradict the code next to them. For each claim, open the code that decides it before judging. Report verified defects only.
