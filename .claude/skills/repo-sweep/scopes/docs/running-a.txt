Documentation check of kamchatka/RUNNING.md from its start to the heading 'a settings file' (lines 1 to about 484): headless mode, a session you can walk away from (remote/), the two dialects, the model and its address, the status line's estimate, embedding it. Check every flag, default, environment variable, exit code and output format against kamchatka/src/args.rs, headless.rs, main.rs, remote/ (client.rs, server.rs, protocol.rs), endpoint.rs, config.rs, and the status line in ui/. For each claim, open the code that decides it before judging. Report verified defects only.
