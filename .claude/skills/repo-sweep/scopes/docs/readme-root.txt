Documentation check of the top-level README.md against the workspace: the crates it lists and what it says each does, version numbers and install commands (each Cargo.toml, kamchatka/CHANGELOG.md, .github/workflows/release.yml: which binaries are attached), the code examples (does every type, method and argument exist with that signature in nachalnik/src), platform claims, links to files and to docs/ pages (do the paths exist). For each claim, open the code that decides it before judging. Report verified defects only.
