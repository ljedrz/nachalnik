Documentation check of the doc comments and note: paragraphs in nachalnik/src outside kernel/: context.rs, model.rs, projection.rs, tool.rs, permissions.rs, tokens.rs, compaction.rs, event.rs, session.rs, config.rs, error.rs, selectors.rs, test.rs and lib.rs (the crate-level docs). For each public item: does its doc say what the code does (defaults, what is returned on each branch, what is refused and with which Error), are the event names and field names in docs the ones in event.rs, do intra-doc links resolve to items that exist, and do module docs describe the current contents of the module. For each claim, open the code that decides it before judging. Report verified defects only.
