Documentation check of the doc comments, note: paragraphs and in-body comments in kamchatka/src files no other scope covers: args.rs, advisor.rs, attach.rs, clipboard.rs, config.rs, help.rs, remote/mod.rs, remote/server.rs, remote/client.rs, remote/protocol.rs. For each: does it say what the code does now - defaults, precedence between flags and the settings file, protocol versions and line limits, what a client may do; do intra-doc links resolve; do comments in bodies contradict the code next to them. For each claim, open the code that decides it before judging. Report verified defects only.
