Documentation check of the doc comments, note: paragraphs and in-body comments in kamchatka/src/tools/ (mod.rs, fs.rs, files.rs, ops.rs, search.rs, shell.rs, policy.rs, reaching.rs, trim.rs, advice.rs). For each: does it say what the code does now - limits and caps and their values, what is refused and the message, what each branch returns, how paths and links are resolved; do intra-doc links resolve; do comments in bodies contradict the code next to them. For each claim, open the code that decides it before judging. Report verified defects only.
