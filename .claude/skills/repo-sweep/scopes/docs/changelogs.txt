Documentation check of the [unreleased] sections of nachalnik/CHANGELOG.md, nachalnik-providers/CHANGELOG.md, nachalnik-eval/CHANGELOG.md and kamchatka/CHANGELOG.md (read each only up to its first released version heading). For each bullet: does the code at HEAD do what it says - open the item it names and check the name, the behaviour and the default; flag bullets that name a type, function, flag or file that does not exist under that name, and two bullets in one section that contradict each other. Also check the versions in each Cargo.toml agree with the latest released heading and that nothing depends on a version of a sibling the registry would not have (Cargo.toml [workspace.dependencies] and each crate's). For each claim, open the code that decides it before judging. Report verified defects only.
