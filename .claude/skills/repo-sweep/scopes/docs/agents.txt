Documentation check of AGENTS.md against the workspace. Check every factual claim: the number of dependencies of nachalnik and which they are (Cargo.toml, nachalnik/Cargo.toml), the six traits and the three minimal implementations it names (nachalnik/src/lib.rs and the seam files), which four seams carry a name(), the state machine diagram and its transitions (nachalnik/src/kernel/mod.rs), the workspace table (each crate's Cargo.toml: published or not, dependencies, platform gates in kamchatka/src/lib.rs or main.rs), the file layout named under 'where things are' (including the nine steps of kamchatka/src/wiring.rs), the commands and what CI runs (.github/workflows/ci.yml, release.yml: features, --no-default-features crates, the keyless examples, MSRV, runners), and the invariants list against INVARIANTS.md. For each claim, open the code that decides it before judging. Report verified defects only.
