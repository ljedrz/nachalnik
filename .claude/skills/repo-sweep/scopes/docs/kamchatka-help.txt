Documentation check of the text kamchatka shows a person or a model, against what the code does: the --help text and each flag's description in kamchatka/src/args.rs; help.rs; the slash-command descriptions in app/command.rs; every tool's name, description and argument schema descriptions in tools/ (fs.rs, shell.rs, search.rs, files.rs, trim.rs, advice.rs, ops.rs) and introspect/ (context/, fork.rs, log.rs, setup.rs) - check each described limit, default, action and argument against the code that parses and runs it; and error messages that name a flag, a key or a limit. A tool description is read by the model on every request, so a wrong limit or a missing action is high. For each claim, open the code that decides it before judging. Report verified defects only.
