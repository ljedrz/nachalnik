Documentation check of MAP.md against the tree. Every file, module, directory, function, type and test it names must exist where it says (use glob and grep), and each file's one-line description must match what the file now does - read the file's module doc and top-level items. Look for files that exist in nachalnik/src, kamchatka/src (including app/, ui/, tools/, introspect/, remote/), nachalnik-providers/src, nachalnik-mcp/src and nachalnik-eval/src that MAP.md describes under an old name or places in the wrong module, and claims about which file owns a responsibility that has moved. For each claim, open the code that decides it before judging. Report verified defects only.
