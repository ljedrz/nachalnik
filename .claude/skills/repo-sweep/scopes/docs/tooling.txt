Documentation check of the comments and usage text in scripts/ (mutate.sh, references.sh, references.allow, windows.sh), .github/workflows/ci.yml and release.yml, every Cargo.toml (the workspace one and each crate's: each dependency's comment saying why must still be true - grep for the dependency's use - and descriptions, keywords, readme and include fields must name files that exist), nachalnik-utils/src/lib.rs and its Cargo.toml, .gitattributes and .gitignore. Also the sweep skill under .claude/skills/repo-sweep: SKILL.md and agent_prompt.md against its scripts (flags, file names, defaults). For each claim, open the code that decides it before judging. Report verified defects only.
