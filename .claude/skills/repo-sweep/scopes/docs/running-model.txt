Documentation check of kamchatka/RUNNING.md lines 296 to 594 only (the model and its address, the status line's estimate, embedding it, a settings file). Check endpoint and model selection against kamchatka/src/endpoint.rs and args.rs, the estimate and its kinds against the status line code in ui/ and nachalnik/src/tokens.rs, embedding against kamchatka/src/lib.rs and wiring.rs, and every settings key listed (name, type, default, how a flag overrides it) against args.rs and config.rs, and against the sample kamchatka/kamchatka.json. For each claim, open the code that decides it before judging. Report verified defects only.
