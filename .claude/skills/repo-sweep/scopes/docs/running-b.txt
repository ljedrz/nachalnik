Documentation check of kamchatka/RUNNING.md from the heading 'a settings file' to its end (lines about 485 to 981): the settings file and every key it lists, options, the colour on the question, an advisor on this machine, what the colour says, starting again, the tests, a toolchain in the home directory, the network when a command tries. Check against kamchatka/src/args.rs (every settings key, its type and default, and how it combines with flags), config.rs, advisor.rs, tools/advice.rs, sandbox.rs, gate.rs, and kamchatka/tests (the live suite's variables). For each claim, open the code that decides it before judging. Report verified defects only.
