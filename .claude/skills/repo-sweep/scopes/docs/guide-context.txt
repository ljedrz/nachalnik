Documentation check of kamchatka/GUIDE.md from line 701 to its end (letting the agent read and manage its own context). Check every action, argument, limit and behaviour of the introspection tools against kamchatka/src/introspect/ (mod.rs, context/mod.rs, context/changes.rs, fork.rs, log.rs, setup.rs) and tools/trim.rs, and what a person sees of it against app/ and ui/. For each claim, open the code that decides it before judging. Report verified defects only.
