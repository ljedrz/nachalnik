Documentation check of kamchatka/examples (attached.rs, gateway.rs, jev_assisted_compaction.rs, phone.rs, recorded.rs, relay/, browser.html) and kamchatka/kamchatka.json, kamchatka/contrib/laya_advisor.py: each example's module doc says what it does and how to run it (features, flags, variables) and matches kamchatka/Cargo.toml's [[example]] entries; comments match the code; every key in kamchatka.json is one args.rs accepts, with a value of the right type; and every place a document names one of these files describes it correctly (grep the .md files for each name). For each claim, open the code that decides it before judging. Report verified defects only.
