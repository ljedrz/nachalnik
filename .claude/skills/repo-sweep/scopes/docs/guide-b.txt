Documentation check of kamchatka/GUIDE.md from the heading 'the permissions tab' to its end (lines about 378 to 895): the permissions tab and the question, a command that reaches for the network, finding things without a shell, putting something in (attachments, clipboard), letting the agent read and manage its own context. Check against kamchatka/src/tools/policy.rs, gate.rs, sandbox.rs, tools/search.rs, tools/fs.rs, attach.rs, clipboard.rs, introspect/ (context/, fork.rs, log.rs, setup.rs), app/keys.rs and app/command.rs: key bindings, answers to a question and what each does, tool names and actions, limits and defaults. For each claim, open the code that decides it before judging. Report verified defects only.
