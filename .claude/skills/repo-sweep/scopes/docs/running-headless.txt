Documentation check of kamchatka/RUNNING.md lines 9 to 295 only (the same program without a screen; a session you can walk away from; two dialects). Check every flag, input line, output record field, exit code and deadline behaviour against kamchatka/src/headless.rs, args.rs and main.rs; attach, detach and the socket against remote/ (mod.rs, server.rs, client.rs, protocol.rs) and kamchatka/examples (attached.rs, phone.rs, gateway.rs, relay, browser.html); the dialects against endpoint.rs and nachalnik-providers. For each claim, open the code that decides it before judging. Report verified defects only.
