Documentation check of kamchatka/RUNNING.md from line 595 to its end (options, a colour on the question, an advisor, what the colour says, starting again, the tests, a toolchain in the home directory, the network). Check against kamchatka/src/advisor.rs, tools/advice.rs, kamchatka/contrib/laya_advisor.py and laya_fit.json, args.rs, sandbox.rs, gate.rs, and kamchatka/tests (live.rs and every suite named, which variables they read). For each claim, open the code that decides it before judging. Report verified defects only.
