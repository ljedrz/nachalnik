Documentation check of the doc comments and note: paragraphs in nachalnik/src/kernel/mod.rs, request.rs and calls.rs. For each public item and each note:, check that it says what the function does now: the states it may be called in and the Error variants it returns (read the body and nachalnik/src/error.rs), the events it emits, whether it takes a checkpoint, what it does with pending calls. Also intra-doc links ([`Foo`], [`Kernel::bar`]) that name an item that has been renamed or no longer exists, and comments inside function bodies that contradict the code next to them. For each claim, open the code that decides it before judging. Report verified defects only.
