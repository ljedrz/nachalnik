Documentation check of SECURITY.md against the code that enforces or reports what it describes: nachalnik/src/permissions.rs and kernel/calls.rs (what the policy sees), kamchatka/src/sandbox.rs, gate.rs, tools/shell.rs, tools/policy.rs, tools/reaching.rs, tools/fs.rs and args.rs (Landlock, the network gate, path rules, --deny, --sandbox-allow, --no-sandbox, what happens on a kernel without Landlock). Failure classes: a protection described as enforced that is only reported or asked about; a flag or rule named with the wrong spelling or default; a description of a platform other than Linux as supported; a claim about what the model's output can reach that the code contradicts. For each claim, open the code that decides it before judging. Report verified defects only.
